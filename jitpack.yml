jdk:
   - openjdk11
before_install:
   - yes | sdkmanager "cmake;3.18.1"
   - wget http://ufpr.dl.sourceforge.net/project/swig/swig/swig-4.0.2/swig-4.0.2.tar.gz
   - tar xvzf swig-4.0.2.tar.gz
   - cd swig-4.0.2
   - wget http://ufpr.dl.sourceforge.net/project/pcre/pcre/8.45/pcre-8.45.tar.gz
   - ./configure --prefix=$HOME/swig
   - make clean && make && make install
   - cd ../
   - git submodule update --init --recursive
   - cd packages
   - ./gradlew assemble
